name: My First 
on: push 

jobs:
  first_job:
    runs-on: ubuntu-latest

    steps:
      - name: welcome message 
        run: echo " my first github job"

      - name: list files
        run: ls

      - name: Read files
        run: cat README.md  
